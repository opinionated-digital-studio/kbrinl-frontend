<details {%- if params.id %} id="{{params.id}}"{% endif %} class="kbrinl-details {%- if params.classes %} {{ params.classes }}{% endif %}" data-module="kbrinl-details" {%- for attribute, value in params.attributes %} {{attribute}}="{{value}}"{% endfor %} {{- " open" if params.open }}>
  <summary class="kbrinl-details__summary">
    <span class="kbrinl-details__summary-text">
      {{ params.summaryHtml | safe if params.summaryHtml else params.summaryText }}
    </span>
  </summary>
  <div class="kbrinl-details__text">
    {{ params.html | safe if params.html else params.text }}
  </div>
</details>
